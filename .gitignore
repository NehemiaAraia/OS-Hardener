__pycache__/
*.pyc
.env
.venv/
venv/
*.egg-info/
.pytest_cache/

# scan artifacts
reports/*.html
reports/*.json
!reports/.gitkeep

# terraform
.terraform/
*.tfstate
*.tfstate.*
terraform.tfvars
*.pem

# scan history db and the remediation audit trail — both are runtime state, and
# a committed audit log reads as real remediation history that never happened
reports/*.db
reports/*.log
